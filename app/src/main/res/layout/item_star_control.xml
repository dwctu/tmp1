<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <View
        android:id="@+id/cl_toy"
        android:background="@drawable/content_icon_pink"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>
    <TextView
        android:textSize="11sp"
        android:textColor="@color/white"
        android:id="@+id/tv_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="lush"
        app:layout_constraintBottom_toBottomOf="@+id/cl_toy"
        app:layout_constraintLeft_toLeftOf="@+id/cl_toy"
        app:layout_constraintRight_toRightOf="@+id/cl_toy"
        app:layout_constraintTop_toTopOf="@+id/cl_toy"/>
    <TextView
        android:textSize="12sp"
        android:textColor="#ffe02020"
        android:id="@+id/tv_status"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/common_reconnecting"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/cl_toy"/>
    <LinearLayout
        android:gravity="bottom|center_horizontal"
        android:orientation="horizontal"
        android:id="@+id/ll_connected_show"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="12dp"
        android:layout_marginTop="6dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/cl_toy">
        <ImageView
            android:id="@+id/toy_battery_img"
            android:layout_width="wrap_content"
            android:layout_height="8dp"
            android:layout_marginRight="10dp"
            android:src="@drawable/battery"/>
        <ImageView
            android:id="@+id/toy_rssi_img"
            android:layout_width="14dp"
            android:layout_height="12dp"
            android:src="@drawable/icon_signal_level4"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
